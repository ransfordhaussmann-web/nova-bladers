--[[
	MatchmakingConfig — queue timing and UI defaults.
]]

local MatchmakingConfig = {
	QUEUE_TICK_INTERVAL = 0.5,
	PENDING_RETRY_INTERVAL = 1,
	QUICK_MATCH_MODE = "auto",
}

return MatchmakingConfig
