local MatchmakingConfig = {
	-- FFA: start when full or after this many seconds once minPlayers is reached
	FFA_FILL_TIMEOUT = 12,
	-- How often the service re-evaluates queues
	QUEUE_TICK = 0.5,
}

return MatchmakingConfig
