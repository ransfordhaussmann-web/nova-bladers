--[[
	MatchmakingConfig — timeouts and defaults for the queue system.
]]

local MatchmakingConfig = {
	FFA_FILL_TIMEOUT = 12,
	ARENA_BUSY_POLL = 0.5,
	DEFAULT_MODE = "training",
}

return MatchmakingConfig
