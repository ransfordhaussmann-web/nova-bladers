--[[
	MatchmakingConfig — tuning values for the queue system.
]]

local MatchmakingConfig = {
	FFA_FILL_TIMEOUT = 12,
	QUEUE_UPDATE_INTERVAL = 0.5,
	PORTAL_DEFAULT_MODE = "training",
}

return MatchmakingConfig
