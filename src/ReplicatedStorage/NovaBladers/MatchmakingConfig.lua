local MatchmakingConfig = {
	-- Wie oft Queue-Status an wartende Spieler gesendet wird (Sekunden)
	QUEUE_BROADCAST_INTERVAL = 1,

	-- Kurze Verzögerung bevor ein bereites Match startet (UI-Feedback)
	MATCH_START_DELAY = 0.5,
}

return MatchmakingConfig
