--[[
	MatchmakingConfig — queue timing and capacity defaults.
]]

local MatchmakingConfig = {
	QUEUE_BROADCAST_INTERVAL = 1,
	FFA_FILL_TIMEOUT = 12,
	FFA_MIN_PLAYERS = 2,
	FFA_MAX_PLAYERS = 6,
}

return MatchmakingConfig
