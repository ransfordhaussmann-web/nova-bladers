--[[
	MatchmakingConfig — queue timing and quick-match behaviour.
]]

local MatchmakingConfig = {
	FFA_FILL_TIMEOUT = 12,
	QUEUE_BROADCAST_INTERVAL = 1,
	PENDING_RETRY_INTERVAL = 2,
}

return MatchmakingConfig
