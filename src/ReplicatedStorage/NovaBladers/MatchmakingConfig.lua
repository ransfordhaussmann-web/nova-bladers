--[[
	MatchmakingConfig — tuning knobs for the queue system.
]]

local MatchmakingConfig = {
	QUEUE_TICK = 0.5,
	FFA_FILL_TIMEOUT = 12,
	FFA_MAX_PLAYERS = 6,
	FFA_MIN_PLAYERS = 3,
}

return MatchmakingConfig
