--[[
	MatchmakingConfig — queue timing and broadcast intervals.
]]

local MatchmakingConfig = {
	FFA_FILL_TIMEOUT = 12,
	QUEUE_BROADCAST_INTERVAL = 0.5,
	PENDING_RETRY_INTERVAL = 1,
}

return MatchmakingConfig
