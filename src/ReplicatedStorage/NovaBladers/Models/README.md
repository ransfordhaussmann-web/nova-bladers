# Bey model imports (optional)

Place imported Creator Store or FBX models here as **Models** children.

| Model name | Bey |
|------------|-----|
| `NovaStriker` | Nova Striker |
| `FrostFang` | Frost Fang |
| `BlazeCrown` | Blaze Crown |

After Studio import: `ReplicatedStorage → NovaBladers → Models → <ModelName>`

See `docs/BEY-MODELS.md` and `docs/SKETCHFAB-NOVA-STRIKER.md` for setup steps.
