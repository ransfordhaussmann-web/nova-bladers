
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store imports (same folder):
- **CrimsonFang** — attack bey mesh
- **AuroraCrest** — defense bey mesh

Set `modelRef.studioModelName` in BeyCatalog.lua matches the model name here.
