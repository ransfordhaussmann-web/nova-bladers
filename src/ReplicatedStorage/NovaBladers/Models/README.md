Import 3D models here for in-game use (optional — procedural fallback if missing).

| Folder | Source |
|--------|--------|
| **NovaStriker** | Sketchfab GLB — see docs/SKETCHFAB-NOVA-STRIKER.md |
| **CrimsonFang** | Creator Store: search `spinning top red attack` |
| **FrostCrown** | Creator Store: search `spinning top ice crystal` |

After Studio import: ReplicatedStorage → NovaBladers → Models → &lt;ModelName&gt;
