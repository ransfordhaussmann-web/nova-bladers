Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store slots (optional)

Import Toolbox models into Studio, then place under `Models/` or set `modelAssets.meshId` in `BeyCatalog.lua`:

| Bey | Studio model name | Search terms |
|-----|-------------------|--------------|
| Nova Striker | NovaStriker | sketchfab import |
| Crimson Blaze | CrimsonBlaze | spinning top, bey, fire top |
| Frost Crown | FrostCrown | spinning top, bey, ice top |

Procedural fallback builds are used when no Studio model is present.
