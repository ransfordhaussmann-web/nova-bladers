
Import 3D models here for in-game use (Studio → ReplicatedStorage → NovaBladers → Models).

| Folder name | Bey | Notes |
|-------------|-----|-------|
| **NovaStriker** | Nova Striker | Sketchfab GLB — see docs/SKETCHFAB-NOVA-STRIKER.md |
| **CrimsonFang** | Crimson Fang | Creator Store or custom FBX import |
| **TitanCore** | Titan Core | Creator Store or custom FBX import |

After import: set `PrimaryPart`, weld parts, optional `Hull` on collision part.
Alternatively paste `modelAssets.meshId` in `BeyCatalog.lua` (see docs/BEY-MODELS.md).
