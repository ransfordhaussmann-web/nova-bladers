
Import Creator Store / custom 3D models here for in-game use.

| Model folder | Bey | Notes |
|--------------|-----|-------|
| `NovaStriker` | Nova Striker | See `docs/SKETCHFAB-NOVA-STRIKER.md` |
| `CrimsonFang` | Crimson Fang | Toolbox → Creator Store → "spinning top attack red" |
| `GlacierShield` | Glacier Shield | Toolbox → Creator Store → "spinning top defense ice" |

After Studio import: `ReplicatedStorage → NovaBladers → Models → <ModelName>`

Set **PrimaryPart** (or child named `Hull`). Target size ~3.5 studs wide, flat on arena floor.
