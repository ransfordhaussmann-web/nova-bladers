# Bey Studio Models

Import Creator Store or custom 3D models here for in-game use.

| Model name | Bey | Notes |
|------------|-----|-------|
| `NovaStriker` | Nova Striker | Sketchfab GLB — see `docs/SKETCHFAB-NOVA-STRIKER.md` |
| `IronShell` | Iron Shell | Optional Creator Store import |
| `VoltDash` | Volt Dash | Optional Creator Store import |
| `ShadowBite` | Shadow Bite | Optional Creator Store import |
| `CrimsonEdge` | Crimson Edge | Optional Creator Store import |
| `FrostHalo` | Frost Halo | Optional Creator Store import |

After Studio import: `ReplicatedStorage → NovaBladers → Models → <studioModelName>`

Alternative: set `modelAssets.meshId` in `BeyCatalog.lua` (Toolbox MeshId).
