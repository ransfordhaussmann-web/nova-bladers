Import Sketchfab GLB or Creator Store models here for in-game use.

| Model folder name | Bey |
|-------------------|-----|
| NovaStriker | Nova Striker |
| CrimsonFang | Crimson Fang |
| FrostRing | Frost Ring |

After Studio import: ReplicatedStorage → NovaBladers → Models → (folder name above)

For Creator Store meshes without import: set `modelAssets.meshId` in `BeyCatalog.lua` (see docs/BEY-MODELS.md).
