
Import 3D models here for in-game use (optional — procedural fallback exists).

| Model name | Bey | Source |
|------------|-----|--------|
| `NovaStriker` | Nova Striker | Sketchfab GLB — see docs/SKETCHFAB-NOVA-STRIKER.md |
| `CrimsonVortex` | Crimson Vortex | Creator Store or custom import |
| `FrostHalo` | Frost Halo | Creator Store or custom import |

After Studio import: `ReplicatedStorage → NovaBladers → Models → <ModelName>`

Studio folder models take priority over Creator Store auto-load.
