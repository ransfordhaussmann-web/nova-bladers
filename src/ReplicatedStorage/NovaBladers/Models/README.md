
Import optional 3D models here for in-game use (Studio → File → Import 3D).

| Model name | Bey |
|------------|-----|
| **NovaStriker** | Nova Striker (Sketchfab — see docs/SKETCHFAB-NOVA-STRIKER.md) |
| **CrimsonFang** | Crimson Fang (Creator Store mesh optional) |
| **FrostCrown** | Frost Crown (Creator Store mesh optional) |

After Studio import: ReplicatedStorage → NovaBladers → Models → `<ModelName>`

Alternatively set `modelAssets.meshId` in `BeyCatalog.lua` (see docs/BEY-MODELS.md).
