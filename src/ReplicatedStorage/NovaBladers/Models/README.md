
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store imports (Toolbox → Creator Store, search terms in BeyCatalog.modelAssets):
- **CrimsonFang** — "spinning top attack red"
- **GraniteFort** — "spinning top defense stone"
- **SolarDrift** — "spinning top stamina sun"
- **PhantomEdge** — "spinning top balance blade"

Procedural fallback models are built automatically when no Studio model is present.
