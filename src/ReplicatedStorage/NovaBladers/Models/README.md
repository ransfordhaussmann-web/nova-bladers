Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store models (same folder):
- **BlazeQuill** — fire attack bey
- **TideAnchor** — water defense bey

Set `modelRef.studioModelName` in BeyCatalog (already configured).
Or paste a Toolbox mesh ID into `modelAssets.meshId` on the catalog entry.
