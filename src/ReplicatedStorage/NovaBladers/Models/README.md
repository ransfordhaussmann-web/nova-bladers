
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store Beys

Optional 3D models from Roblox Creator Store (Toolbox):

| Folder | Bey | Notes |
|--------|-----|-------|
| `CrimsonFang` | Crimson Fang | Attack — red fang blades |
| `FrostHalo` | Frost Halo | Defense — ice halo ring |

Import a spinning-top model into Studio, rename to match `studioModelName` in `BeyCatalog.lua`,
then place under `ReplicatedStorage/NovaBladers/Models/`. Procedural fallback builds if missing.
