
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store models:
- **CrimsonFang** — attack bey (Ember Starfall)
- **FrostCrown** — defense bey (Glacier Lock)

Set `modelAssets.meshId` in `BeyCatalog.lua` or place a Model in this folder.
