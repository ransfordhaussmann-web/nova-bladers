
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store imports:
- **CrimsonFang** — attack bey (red spinning top)
- **AuroraCrest** — defense bey (blue/purple crest top)

Set `modelRef.studioModelName` in BeyCatalog.lua to match folder name.
Or use `modelAssets.meshId` for direct MeshPart loading.
