
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store meshes

Optional: paste `rbxassetid` from Toolbox into `BeyCatalog.modelAssets.meshId` for:

- CrimsonFang
- GraniteFort
- SolarDrift
- PhantomEdge

Without meshId, procedural fallback models are used automatically.
