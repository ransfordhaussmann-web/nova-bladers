
Optional Creator Store imports (ReplicatedStorage → NovaBladers → Models):

| Model name   | Creator Store search hint              |
|--------------|----------------------------------------|
| NovaStriker  | Sketchfab import — see docs/SKETCHFAB-NOVA-STRIKER.md |
| CrimsonFang  | spinning top attack red blade          |
| GraniteFort  | spinning top defense stone shield      |
| SolarDrift   | spinning top stamina gold ring         |
| PhantomEdge  | spinning top balance cyan edge         |

Paste `rbxassetid` from Toolbox into `BeyCatalog.modelAssets.meshId` for external meshes.
