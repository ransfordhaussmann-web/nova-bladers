
Import Creator Store / Sketchfab models here for in-game use.

| Studio model name | Bey ID |
|-------------------|--------|
| NovaStriker | NovaStriker |
| CrimsonFang | CrimsonFang |
| FrostHalo | FrostHalo |

After Studio import: ReplicatedStorage → NovaBladers → Models → `<ModelName>`

Optional: set `modelAssets.meshId` in BeyCatalog for rbxassetid mesh fallback.
