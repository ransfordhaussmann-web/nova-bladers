
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store Beys

Optional Studio imports for store beys (procedural fallback if missing):

| Folder | Bey |
|--------|-----|
| `CrimsonFang` | Crimson Fang (Attack) |
| `FrostHalo` | Frost Halo (Defense) |

Toolbox → Creator Store → search "spinning top" → import → rename folder to match.
Or set `modelAssets.meshId` in `BeyCatalog.lua`.
