
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional folders for Creator Store imports:
- **CrimsonRift** — attack spinning top mesh
- **FrostLedger** — ice/defense spinning top mesh

Set `modelAssets.meshId` in `BeyCatalog.lua` or place a Model named after the bey id.
