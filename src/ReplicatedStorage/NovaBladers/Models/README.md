
Import Creator Store or custom 3D models here for in-game use.

| Folder | Bey |
|--------|-----|
| `NovaStriker` | Nova Striker (see docs/SKETCHFAB-NOVA-STRIKER.md) |
| `BlazeLance` | Blaze Lance — optional Creator Store mesh |
| `CoralTide` | Coral Tide — optional Creator Store mesh |

After Studio import: ReplicatedStorage → NovaBladers → Models → <ModelName>
Procedural fallback builds automatically when a folder is missing.
