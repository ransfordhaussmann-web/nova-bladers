
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store (optional)

For other Beys, search Roblox Studio Toolbox → Creator Store using the
`modelAssets.creatorStoreSearch` hint in `BeyCatalog.lua`, then paste the
`rbxassetid` into `modelAssets.meshId` for that Bey.
