Import optional 3D models here for in-game use (Creator Store or own FBX/GLB).

| Model name | Bey | Notes |
|------------|-----|-------|
| `NovaStriker` | Nova Striker | See docs/SKETCHFAB-NOVA-STRIKER.md |
| `BlazeQuill` | Blaze Quill | Creator Store or custom import |
| `TideAnchor` | Tide Anchor | Creator Store or custom import |

After Studio import: ReplicatedStorage → NovaBladers → Models → `<ModelName>`

Alternatively set `modelAssets.meshId` in `BeyCatalog.lua` (see docs/BEY-MODELS.md).
