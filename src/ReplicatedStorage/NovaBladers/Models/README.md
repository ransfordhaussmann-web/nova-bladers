# Bey model imports (Creator Store / Sketchfab)

Place imported Studio models here. `BeyModelBuilder` clones by `modelRef.studioModelName` (falls back to bey `id`).

| Folder name | Bey |
|-------------|-----|
| `NovaStriker` | Nova Striker (see docs/SKETCHFAB-NOVA-STRIKER.md) |
| `IronShell` | Iron Shell |
| `VoltDash` | Volt Dash |
| `ShadowBite` | Shadow Bite |
| `CrimsonFang` | Crimson Fang |
| `FrostRing` | Frost Ring |

After import: set **PrimaryPart** (or child `Hull`), scale ~3.5 studs wide, flat on arena.
