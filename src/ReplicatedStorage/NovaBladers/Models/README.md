
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

Optional Creator Store imports:
- **CrimsonComet** — attack bey (flat on ground, ~3.5 studs)
- **FrostRegent** — defense bey (flat on ground, ~3.6 studs)

Set `modelRef.studioModelName` in BeyCatalog.lua or paste `modelAssets.meshId`.
