
Import optional Creator Store / custom 3D models here for in-game use.

| Folder | Bey | Notes |
|--------|-----|-------|
| `NovaStriker` | Nova Striker | Sketchfab import — see `docs/SKETCHFAB-NOVA-STRIKER.md` |
| `CrimsonFang` | Crimson Fang | Creator Store spin top → import as Model |
| `FrostHalo` | Frost Halo | Creator Store spin top → import as Model |

After Studio import: `ReplicatedStorage → NovaBladers → Models → <ModelName>`

If no model is present, procedural 3D layers are built at runtime from `BeyModelBuilder.lua`.
