# Bey Model Imports

Optional Studio-imported meshes replace procedural visuals when present here.

| Folder name | Bey |
|-------------|-----|
| `NovaStriker` | Nova Striker |
| `IronShell` | Iron Shell |
| `VoltDash` | Volt Dash |
| `ShadowBite` | Shadow Bite |
| `CrimsonVortex` | Crimson Vortex |
| `GlacierRing` | Glacier Ring |

## Creator Store (meshId)

Alternatively set `modelAssets.meshId` in `BeyCatalog.lua` — see `docs/BEY-MODELS.md`.

## Sketchfab import

Nova Striker: see `docs/SKETCHFAB-NOVA-STRIKER.md`

After Studio import: `ReplicatedStorage → NovaBladers → Models → <BeyId>`
