
Import Sketchfab GLB or Creator Store meshes here for in-game use.

| Model name | Bey |
|------------|-----|
| **NovaStriker** | Nova Striker (Sketchfab — see docs/SKETCHFAB-NOVA-STRIKER.md) |
| **CrimsonFlare** | Crimson Flare (optional Creator Store mesh) |
| **FrostCrown** | Frost Crown (optional Creator Store mesh) |

After Studio import: ReplicatedStorage → NovaBladers → Models → `<ModelName>`
Or set `modelAssets.meshId` in `BeyCatalog.lua`.
