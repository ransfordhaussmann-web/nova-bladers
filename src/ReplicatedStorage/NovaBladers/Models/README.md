
Import Sketchfab GLB here as **NovaStriker** for in-game use.
See docs/SKETCHFAB-NOVA-STRIKER.md

After Studio import: ReplicatedStorage → NovaBladers → Models → NovaStriker

## Creator Store Beys

Optional Studio imports for store beys (procedural fallback built at runtime):

| Model folder | Bey | Notes |
|--------------|-----|-------|
| `CrimsonFang` | Crimson Fang | Attack store bey |
| `FrostHalo` | Frost Halo | Defense store bey |

Import from Creator Store → place model here → set `PrimaryPart` + `Hull` on collision part.
