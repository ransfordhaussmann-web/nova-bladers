# Frost Crown — optional Studio import

Place a Creator Store / imported spinning-top model here as **FrostCrown** (Model).

Path: `ReplicatedStorage → NovaBladers → Models → FrostCrown`

Or set `modelAssets.meshId` in `BeyCatalog.lua` with a Toolbox rbxassetid.
