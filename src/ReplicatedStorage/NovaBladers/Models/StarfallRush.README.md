# Starfall Rush — optional Studio import

Place a Creator Store / imported spinning-top model here as **StarfallRush** (Model).

Path: `ReplicatedStorage → NovaBladers → Models → StarfallRush`

Or set `modelAssets.meshId` in `BeyCatalog.lua` with a Toolbox rbxassetid.
